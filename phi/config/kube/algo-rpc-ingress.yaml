apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: algo-grpc-ingress
spec:
  rules:
    - host: kube-dns
      http:
        paths:
          - backend:
              serviceName: algo-grpc-server
              servicePort: 14390
